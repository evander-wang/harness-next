---
name: deliver-node-change
description: 汇总 Node.js TypeScript 变更、验证证据和剩余风险。
---

# 交付 Node.js TypeScript 变更

根据已完成 Step 和 Runtime 保存的证据生成 Workflow Output。

输出必须包含 `status: done`、简明摘要、实际变更文件、验证证据和剩余风险。不得把未执行的命令写成已通过。
