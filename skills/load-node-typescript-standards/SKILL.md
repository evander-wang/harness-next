---
name: load-node-typescript-standards
description: 读取标准 Workflow 的完整 Node.js TypeScript 开发规范。
---

# 加载 Node.js TypeScript 开发规范

完整阅读 `harness/workflows/node-typescript-standards/STANDARDS.md`。将正文的全部约束保留为当前 Agent 任务的开发上下文。

本节点不分析需求、不修改代码、不执行检查。完成后只提交已读取标准版本和文件路径的证据。
